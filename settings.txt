# CellAppCP3 参数配置
# 修改后保存即可，下次运行自动读取

# === Cellpose 模型参数 ===
diameter = 120
flow_threshold = 0.95
cellprob_threshold = 1.0
min_size = 200
niter = 200

# === 过滤参数 ===
hull_comp = 0.85
circle_comp = 0.65
dark_threshold = 15
area_ratio = 0.15
circularity = 0.5
min_pixels = 50
brightness_top_pct = 0.05

# === Top 排行 ===
top_n = 3

# === 大图缩放 ===
# 超过此像素面积的图片自动等比例缩放
resize_area_threshold = 9000000
# 缩放后最长边像素
resize_max_side = 4096

# === 输出样式 ===
crop_pad = 2.0
contour_thickness = 2
font_scale = 0.7

# === 排序方向 (1=从亮到暗/荧光, 0=从暗到亮/亮场) ===
sort_descending = 1

# === 排序开关 (1=开启, 0=不排序) ===
enable_sort = 1

# === 坏细胞检测总开关 ===
enable_bad_detection = 1

# === 内圈整体暗区检测（HOLE）===
# 亮度低于此值视为暗像素
bad_dark_threshold = 60
# 内圈（半径 bad_inner_ratio 以内）暗像素占比超过此值判为 HOLE
bad_dark_ratio = 0.05
bad_inner_ratio = 0.7

# === 角度分区暗区检测（HOLE/BROKEN）===
# 把细胞外圈按角度均分的扇区数量
bad_sector_count = 12
# 检测区域：细胞半径的此比例到 1.0 之间的环形区
bad_sector_inner = 0.6
# 某扇区暗占比超过平均值此倍数判为异常扇区
bad_sector_ratio = 1.6
# 异常扇区数达到此值判为 HOLE（局部边缘虫蛀）
bad_sector_min_count = 1
# 异常扇区数达到此值判为 BROKEN（大面积破损）
bad_broken_sector_count = 4

# === 边缘细胞排除 ===
# 距图像边界此像素内的细胞跳过角度分区检测
bad_edge_margin = 20

# === Cellpose 模型名称 (cyto3 / cpsam 等) ===
# 本地有同名文件时优先加载本地，否则使用内置模型
model_name = cpsam
