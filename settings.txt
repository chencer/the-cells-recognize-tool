# CellAppCP3 参数配置
# 修改后保存即可，下次运行自动读取

# === Cellpose 模型参数 ===
diameter = 120
flow_threshold = 0.95
cellprob_threshold = 1.0
min_size = 200
niter = 200

# === 过滤参数 ===
hull_comp = 0.85
circle_comp = 0.65
dark_threshold = 15
area_ratio = 0.15
circularity = 0.5
min_pixels = 50
brightness_top_pct = 0.05

# === Top 排行 ===
top_n = 3

# === 大图缩放 ===
# 超过此像素面积的图片自动等比例缩放
resize_area_threshold = 9000000
# 缩放后最长边像素
resize_max_side = 4096

# === 输出样式 ===
crop_pad = 2.0
contour_thickness = 2
font_scale = 0.7

# === 排序方向 (1=从亮到暗/荧光, 0=从暗到亮/亮场) ===
sort_descending = 1

# === 排序开关 (1=开启, 0=不排序) ===
enable_sort = 1

# === 坏细胞检测 ===
# 内部暗区检测：内部像素低于此值的比例超过 bad_dark_ratio 则判为坏细胞
bad_dark_threshold = 60
bad_dark_ratio = 0.10
# 形状破损检测：凸包完整度低于此值判为坏细胞
bad_hull_threshold = 0.90
# 坏细胞检测总开关 (1=开启, 0=关闭)
enable_bad_detection = 1
# 暗区判定只看内圈的比例（排除外圈黑边，0.85 = 内 85%，覆盖边缘暗点）
bad_inner_ratio = 0.85

# === 边缘破损检测 ===
# 凸包缺陷深度超过细胞半径此比例判为破损
bad_defect_depth_ratio = 0.15
# 凸包面积与细胞面积差超过此比例判为破损
bad_defect_area_ratio = 0.05

# === Cellpose 模型名称 (cyto3 / brightfield / phase3D) ===
# 本地有同名文件时优先加载本地，否则使用内置模型
model_name = cpsam
