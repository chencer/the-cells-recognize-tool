# CellAppCP3 参数配置
# 修改后保存即可，下次运行自动读取

# === Cellpose 模型参数 ===
diameter = 120
flow_threshold = 0.95
cellprob_threshold = 1.0
min_size = 200
niter = 200

# === 过滤参数 ===
hull_comp = 0.85
circle_comp = 0.65
dark_threshold = 15
area_ratio = 0.15
circularity = 0.5
min_pixels = 50
brightness_top_pct = 0.05

# === Top 排行 ===
top_n = 3

# === 大图缩放 ===
# 超过此像素面积的图片自动等比例缩放
resize_area_threshold = 9000000
# 缩放后最长边像素
resize_max_side = 4096

# === 输出样式 ===
crop_pad = 2.0
contour_thickness = 2
font_scale = 0.7

# === 排序方向 (1=从亮到暗/荧光, 0=从暗到亮/亮场) ===
sort_descending = 1

# === 排序开关 (1=开启, 0=不排序) ===
enable_sort = 1

# === 坏细胞检测总开关 ===
enable_bad_detection = 1

# === 暗区连通域检测 ===
# 亮度低于此值视为暗像素
bad_dark_threshold = 60
# 距图像边界此像素内的细胞跳过检测
bad_edge_margin = 20
# 暗块最小面积（占细胞面积比例，小于此值忽略）
bad_blob_min_area = 0.01
# 紧凑度阈值（面积/外接矩形面积，高于此值判为团块而非暗环）
bad_blob_compactness = 0.45
# 最大团块面积占细胞比例超过此值判为 HOLE
bad_hole_area = 0.02
# 最大团块面积占细胞比例超过此值判为 BROKEN
bad_broken_area = 0.10

# === Cellpose 模型名称 (cyto3 / cpsam 等) ===
# 本地有同名文件时优先加载本地，否则使用内置模型
model_name = cpsam
