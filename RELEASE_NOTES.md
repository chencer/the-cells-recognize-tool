# CellAppCP3 v2.4.0 发布说明

CellAppCP3 是一个用于荧光/亮场细胞图像分析的高效工具，在本次更新至 v2.4.0 版本中，我们引入了先进的 Cellpose v4 引擎，并增强了坏细胞检测功能，显著提升了数据分析的准确性和效率。

---

## 工作流程

1. 扫描 `input/` 目录读取所有图片；
2. 超过阈值的大图自动等比例缩放到最长边 4096px；
3. Cellpose 模型对图像进行细胞轮廓识别；
4. 四步过滤链（完整度 → 面积 → 圆形度 → 亮度计算）；
5. 坏细胞检测（HOLE 内部空洞 / BROKEN 形状破碎）；
6. 三色描边标注（红=坏细胞 / 绿=Top 排名 / 黄=普通）；
7. 输出 `result.png`、`data.csv`、`data_original.csv` 和 `crop/` 裁剪图。

---

## 更新内容

- 升级 Cellpose 从 v3 到 v4.1.1，支持 cpsam 模型
- 新增坏细胞检测（HOLE 空洞 + BROKEN 形状破碎）
- 三色描边标注：红=坏细胞、绿=Top 排名、黄=正常
- 新增亮场排序模式（由暗到亮，适合含菌细胞检测）
- CSV 输出新增状态、标记原因、空洞比例、缺口比例列
- 新增 `enable_sort` 排序开关，可关闭自动排名
- 大图处理改为等比缩放（替换原有分块逻辑），同时输出缩放坐标和原图坐标两份 CSV
- 移除诊断图（`_raw_detections.png`）和 Top N CSV 冗余输出

---

## 坏细胞检测说明

本版本新增 `_detect_defects()` 共用函数，对每个候选细胞执行两项检测：

- **HOLE（空洞）**：使用连通域分析检测内部暗区，基于相对亮度阈值，排除外圈黑边（默认内圈 60% 区域）
- **BROKEN（形状破碎）**：使用凸包缺陷深度比判断轮廓破损程度

两项检测任意触发即标记为坏细胞（红色描边），标记原因写入 CSV。

---

## 碰到的问题

1. **MPS 显存不足大图崩溃** → 超大图无法整图送入模型 → 分块处理后改为等比缩放。
2. **超宽拼接图自动直径估算偏小** → 黑边干扰 Cellpose 自动估算 → 固定 `diameter=120`。
3. **超宽拼接图识别不全** → 整图尺寸超出模型感受野 → 分块再合并（现改缩放）。
4. **cyto3 在亮场图精度不足** → cyto3 训练数据以荧光为主 → 改用 cpsam 模型。
5. **cpsam 在 MPS 上慢** → SAM 架构计算量大 → 建议用 CUDA 或 CPU。
6. **坏细胞暗区判定包含外圈黑边** → 整体像素统计包含细胞外边缘 → 改为只看内圈 60% 区域，用相对亮度阈值。

---

## 效果

荧光细胞识别工具能够精准地在圆形细胞中找到并依据其亮度进行排序；对亮场图中的含菌细胞与正常细胞进行区分，并按照暗度进行排名；同时支持处理超大尺寸图片（最大约 1 亿像素），通过等比缩放技术使处理过程更加高效。

---

## 完整参数说明

| 参数 | 默认值 | 说明 |
|------|--------|------|
| `diameter` | 120 | Cellpose 细胞直径估算值（像素） |
| `flow_threshold` | 0.95 | 流场阈值，越高越严格 |
| `cellprob_threshold` | 1.0 | 细胞概率阈值 |
| `min_size` | 200 | 最小细胞像素数 |
| `resample` | 0 | 是否重采样（0=关闭） |
| `niter` | 200 | 迭代次数 |
| `hull_comp` | 0.85 | 凸包完整度阈值（Step1） |
| `circle_comp` | 0.65 | 最小外接圆填充率阈值（Step1） |
| `area_ratio` | 0.15 | 面积下限（相对中位数，Step2） |
| `circularity` | 0.5 | 圆形度阈值（Step3） |
| `min_pixels` | 50 | 亮度采样最小像素数 |
| `brightness_top_pct` | 0.05 | 取最亮前 5% 像素均值（Step4） |
| `top_n` | 3 | 绿色高亮的 Top N 数量 |
| `resize_area_threshold` | 9000000 | 超过此面积（像素²）触发缩放 |
| `resize_max_side` | 4096 | 缩放后最长边像素数 |
| `crop_pad` | 2.0 | 裁剪图边距倍数（相对半径） |
| `contour_thickness` | 2 | 描边宽度（像素） |
| `font_scale` | 0.7 | 标注字体大小 |
| `sort_descending` | 1 | 1=由亮到暗（荧光），0=由暗到亮（亮场） |
| `enable_sort` | 1 | 排序开关，0=不排序不标 Top N |
| `model_name` | cpsam | 模型名称，本地同名文件优先 |
| `bad_dark_threshold` | 60 | 暗区绝对像素阈值（备用） |
| `bad_dark_ratio` | 0.10 | 暗区占比上限 |
| `bad_hull_threshold` | 0.90 | 凸包完整度下限（坏细胞判定） |
| `enable_bad_detection` | 1 | 坏细胞检测总开关 |
| `bad_inner_ratio` | 0.6 | 暗区检测只看内圈比例 |

---

## 版本历史

| 版本 | 日期 | 说明 |
|------|------|------|
| v2.4.0 | 2026-07-22 | Cellpose v4 升级 + 坏细胞检测 + 等比缩放 |
| v2.3.0 | 2026-05-13 | 亮场支持 + 排序模式 + settings.txt |
| v2.2.0 | 2026-05-08 | 大图分块 + CLI 重构 |
| v2.1.0 | 2026-04-22 | CSV 输出 + Top N 裁剪图 |
| v2.0.0 | 2026-04-22 | OLED 暗色 UI 重设计 |
