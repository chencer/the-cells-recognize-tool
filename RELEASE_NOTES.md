# CellAppCP3 v2.5.0 发布说明

CellAppCP3 是一个用于荧光/亮场细胞图像分析的高效工具。v2.5.0 以等比例缩放替代分块识别，优化了坏细胞检测逻辑，并确定了当前最优参数配置。

---

## 工作流程与原理

1. 扫描 `input/` 目录读取所有图像文件（支持 jpg/png/tif）；
2. 超过 `resize_area_threshold`（默认 9M 像素）的大图自动等比缩放到最长边 `resize_max_side`（默认 4096px）；
3. Cellpose（cpsam 模型）对缩放后图像进行细胞轮廓识别，输出 mask；
4. 四步过滤链：完整度（凸包/圆形匹配度）→ 面积（相对中位数）→ 圆形度 → 亮度采样；
5. 坏细胞检测：HOLE 检测内圈暗像素连通域，BROKEN 检测凸包缺陷深度和面积差，边缘细胞自动跳过 BROKEN；
6. 三色标注：红=坏细胞附标注原因，绿=Top 排名附编号和亮度，黄=其余正常细胞；
7. 输出 `result.png`、`data.csv`（缩放坐标）、`data_original.csv`（原图坐标）和 `crop/` 裁剪图；
8. 打印统计数据（检测总数 / 有效数 / Top 1 位置和亮度）。

---

## 坏细胞检测原理

HOLE 检测通过分析内圈（细胞半径 85% 以内）的暗像素连通域占比来判断是否存在内部空洞，占比超过 10% 判为 HOLE。BROKEN 检测依据凸包最大缺陷深度超过半径 8%，或凸包面积与细胞面积之差超过 3% 的标准判定结构破损。边缘排除功能在图像边界内 20px 范围内跳过 BROKEN 检查，避免轮廓截断产生假缺口。

---

## 当前进度

- 等比例缩放替代分块识别（性能提升，大图不再需要 IoU 去重）
- Cellpose v4（cpsam 模型），支持本地模型文件优先加载
- 坏细胞检测：HOLE（内圈暗区）+ BROKEN（凸包缺口/面积差）
- 三色描边标注 + 双 CSV 输出（缩放坐标 + 原图坐标）
- 边缘细胞自动排除 BROKEN 判定

---

## 碰到的问题清单

- **MPS 显存不足大图崩溃** → 超大图整图推理超出 GPU 内存 → 改为等比例缩放处理
- **cyto3 在亮场图精度不足** → cyto3 训练数据以荧光为主 → 升级至 cpsam 模型
- **cpsam 推理慢** → SAM 架构计算量大 → 等比例缩放降低输入分辨率
- **坏细胞暗区判定全部误判** → 检测范围包含外圈天然暗环 → 改为只检测内圈 85% 区域
- **凸包缺陷阈值过低导致大量误判** → 紧密堆积细胞边界本身凹凸不平 → 调高阈值至 0.08，保守判断
- **边缘暗区检测全部误判** → 亮场图细胞外圈本身就是暗环 → 废弃该方法

---

## 已知局限

- BROKEN 检测在紧密堆积的细胞上准确率有限，中等破损度细胞容易漏检
- 调低阈值会导致正常细胞大量误判
- 后续计划：角度分区暗区检测替代凸包缺陷检测

---

## 效果

- 亮场图 20664×8232 缩放至最长边 4096px 后正常处理
- HOLE 检测准确，虫蛀和内部暗点识别可靠
- BROKEN 检测保守（漏检多于误判），避免正常细胞误标为坏细胞

---

## 完整参数说明

| 参数 | 默认值 | 说明 | 建议范围 |
|------|--------|------|---------|
| `diameter` | 120 | 细胞直径（像素） | 50–200 |
| `flow_threshold` | 0.95 | 流场一致性，越低越宽松 | 0.1–1.0 |
| `cellprob_threshold` | 1.0 | 细胞概率门槛，越低检测越多 | -6.0–6.0 |
| `min_size` | 200 | 最小细胞面积（像素） | 50–500 |
| `niter` | 200 | 迭代次数 | 50–400 |
| `model_name` | cpsam | 模型名称，本地文件优先 | cpsam/cyto3 |
| `hull_comp` | 0.85 | 凸包完整度阈值（Step1） | 0.6–0.95 |
| `circle_comp` | 0.65 | 圆形完整度阈值（Step1） | 0.4–0.85 |
| `area_ratio` | 0.15 | 面积下限（相对中位数，Step2） | 0.05–0.3 |
| `circularity` | 0.5 | 圆形度阈值（Step3） | 0.3–0.7 |
| `min_pixels` | 50 | 亮度采样最少像素数 | 20–100 |
| `brightness_top_pct` | 0.05 | 取最亮前 5% 像素均值（Step4） | 0.01–0.1 |
| `top_n` | 3 | 绿色高亮 Top N 数量 | 1–20 |
| `resize_area_threshold` | 9000000 | 超过此面积触发缩放 | 4M–20M |
| `resize_max_side` | 4096 | 缩放后最长边像素 | 2048–8192 |
| `sort_descending` | 1 | 1=由亮到暗（荧光），0=由暗到亮（亮场） | 0/1 |
| `enable_sort` | 1 | 排序开关，0=不标 Top N | 0/1 |
| `crop_pad` | 2.0 | 裁剪图边距倍数 | 1.5–3.0 |
| `contour_thickness` | 2 | 描边宽度（像素） | 1–5 |
| `font_scale` | 0.7 | 标注字体大小 | 0.3–1.5 |
| `enable_bad_detection` | 1 | 坏细胞检测总开关 | 0/1 |
| `bad_dark_threshold` | 60 | 暗区绝对亮度阈值 | 30–100 |
| `bad_dark_ratio` | 0.10 | 内圈暗像素占比上限（HOLE） | 0.05–0.3 |
| `bad_inner_ratio` | 0.85 | 暗区检测内圈比例 | 0.6–0.95 |
| `bad_defect_depth_ratio` | 0.08 | 凸包缺陷深度比上限（BROKEN） | 0.05–0.2 |
| `bad_defect_area_ratio` | 0.03 | 凸包面积差比上限（BROKEN） | 0.02–0.1 |
| `bad_edge_margin` | 20 | 边缘排除距离（像素） | 10–50 |

---

## 版本历史

| 版本 | 日期 | 说明 |
|------|------|------|
| v2.5.0 | 2026-07-23 | 等比例缩放替代分块，坏细胞检测优化（最佳参数固定） |
| v2.4.0 | 2026-07-22 | Cellpose v4 升级，坏细胞检测（HOLE + BROKEN），三色标注 |
| v2.3.0 | 2026-05-13 | settings.txt 配置化，亮场排序，Top N 裁剪图 |
| v2.2.0 | 2026-05-08 | 大图分块处理，CLI 重构，input/results 目录结构 |
| v2.1.1 | 2026-04-23 | 修复超宽图识别，固定 diameter=120 |
| v2.0.0 | 2026-04-22 | OLED 暗色 UI 重设计 |
